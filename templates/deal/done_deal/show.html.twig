{% extends 'base.html.twig' %}

{% block title %}DoneDeal{% endblock %}

{% block body %}
    <h1>DoneDeal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ done_deal.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ done_deal.date ? done_deal.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('deal_done_deal_index') }}">back to list</a>

    <a href="{{ path('deal_done_deal_edit', {'id': done_deal.id}) }}">edit</a>

    {{ include('deal_done_deal/_delete_form.html.twig') }}
{% endblock %}
