{% extends 'base.html.twig' %}

{% block title %}Edit Cities{% endblock %}

{% block body %}
    <h1>Edit Cities</h1>

    {{ include('cities/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('cities_index') }}">back to list</a>

    {{ include('cities/_delete_form.html.twig') }}
{% endblock %}
