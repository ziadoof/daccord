{% form_theme form 'bootstrap_3_layout.html.twig' %}
{{ form_start(form, {'attr': {'id': 'voyage-request'},'method': 'post', 'action': path('voyage_request_new', {'voyage': voyage.id}) }) }}
    {{ form_widget(form) }}
    <button class="btn btn-info float-right mb-4">{{ button_label|default('Send') }}</button>
{{ form_end(form) }}
