{% extends 'base.html.twig' %}

{% block title %}VoyageRequest{% endblock %}

{% block body %}
    <h1>VoyageRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voyage_request.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ voyage_request.status }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ voyage_request.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('carpool_voyage_request_index') }}">back to list</a>

    <a href="{{ path('carpool_voyage_request_edit', {'id': voyage_request.id}) }}">edit</a>

    {{ include('carpool_voyage_request/_delete_form.html.twig') }}
{% endblock %}
