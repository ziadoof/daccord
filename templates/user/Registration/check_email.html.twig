{% extends "layout_visitor.html.twig" %}
{% block content %}
{% block fos_user_content %}
    <div class="container pt-lg-5 pt-3">
        <div class="row mb-md-3 mb-lg-3">
            <div class="col-12">
                <h1 class="welcome-text mt-3">{% trans %}Welcome to FORDEALING{% endtrans %}</h1>
            </div>
        </div>
        <hr>
        <div class="row justify-content-center formLogin mt-4">
            {#<p>{{ 'registration.check_email'|trans({'%email%': user.email}) }}</p>#}
            <p>{% trans %}An email has been sent to {% endtrans%} {{ ' ' }}{{ user.email }}{{ ' ' }}.{% trans%} It contains an activation link you must click to activate your account.{% endtrans %}</p>
        </div>
    </div>

{% endblock fos_user_content %}
{% endblock %}
