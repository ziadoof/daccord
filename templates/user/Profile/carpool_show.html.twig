{% block content %}
    {% if app.user.carpool %}
    <div class="fos_user_user_show px-4 py-4">
        <div class="row">
            <div class="col-xl-4 offset-xl-4 col-md-6 offset-md-3 col-8 offset-2 btn-group ">
                <div class=" carpool-edit ml-2  ">
                    <img class="mb-3 " src="{{ app.user.carpool.photoCarpool() }}" alt="User">
                </div>
            </div>
        </div>
        <div class="justify-content-center">
            <h3 class="text-center pt-3">{{ user.firstname|title }} {{ user.lastname|upper }}</h3>
            <h3 class="pt-3 bdg-accord ">{{ user.carpool.point }} <span>Points</span></h3>
        </div>
        <div class="justify-content-center">
            <div class="text-center pt-2">
                <div class="col">
                    <h6 class="d-inline font-weight-bold">{% trans %}Car brand : {% endtrans %} </h6><span>  {{ app.user.carpool.carBrand }}</span>
                </div>
                <div class="col">
                    <h6 class="d-inline font-weight-bold">{% trans %}Car color : {% endtrans %} </h6><span>  {{ app.user.carpool.carColor }}</span>
                </div>
                <div class="col ">
                    <h6 class="d-inline font-weight-bold">{% trans %}Number of passengers  : {% endtrans %} </h6><span>  {{ app.user.carpool.numberOfPassengers}}</span>
                </div>
                <h6 class="text-center rosed mt-4">{% trans %}Possibility to{% endtrans %}</h6>
                <div class="col">
                    {% if app.user.carpool.bag %}
                        <span class="badge badge-info py-2 px-2 mr-1">{% trans %}Bag{% endtrans %}</span>
                    {% endif %}
                    {% if app.user.carpool.animal %}
                        <span class="badge badge-info py-2 px-2 mr-1">{% trans %}Animal{% endtrans %}</span>
                    {% endif %}
                    {% if app.user.carpool.baby %}
                        <span class="badge badge-info py-2 px-2 mr-1">{% trans %}Baby{% endtrans %}</span>
                    {% endif %}
                    {% if app.user.carpool.bankCard %}
                        <span class="badge badge-info py-2 px-2 mr-1">{% trans %}bank card{% endtrans %}</span>
                    {% endif %}
                </div>
                <h6 class="text-center rosed mt-4">{% trans %}I can share{% endtrans %}</h6>
                <div class="col">
                    {% if app.user.carpool.conversation %}
                        <span class="badge badge-warning py-2 px-2 mr-1">{% trans %}Conversation{% endtrans %}</span>
                    {% endif %}
                    {% if app.user.carpool.music %}
                        <span class="badge badge-warning py-2 px-2 mr-1">{% trans %}Music{% endtrans %}</span>
                    {% endif %}
                </div>
            </div>
        </div>
        {# render rating of driver#}
        {{ render(controller('App\\Controller\\User\\ProfileController::ratingCarpool',{ 'user': app.user })) }}
    </div>
    {% else %}
    <div class="col-md-4 offset-md-4 mt-5">
        <div class="row border-driver">
            <div class="col my-4 mx-2 text-center">
                <h4 class="title-blue my-1 mx-1">{% trans %}Share your car in simple steps{% endtrans %}</h4>
                <p>{% trans %}Registering yourself with the Carpool service will allow you to share your car with other people to get them to your same destination You can get subscribers if your prices are competitive{% endtrans %}</p>
            </div>
        </div>
        <div class="row text-center ">
            <a href="{{ path('fos_user_profile_edit') }}" class="btn btn-info btn-block">{% trans %}Share your car{% endtrans %}</a>
        </div>
    </div>
    {% endif %}
{% endblock %}

