{% extends 'layout_user.html.twig' %}

{% block title %}ADMIN{% endblock %}

{% block content %}
    <div class="row">
        <div class="btn-group btn-block" id="meetup-button">
            <a class="btn btn-info" href="{{path('admin_index')}}" >Dashboard</a>
            <div class="dropdown show">
                <a class="btn btn-info dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    All Users
                </a>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                    <a class="dropdown-item" href="{{path('user_index')}}" >Users</a>
                    <a class="dropdown-item" href="{{ path('driver_index') }}">Driver users</a>
                    <a class="dropdown-item" href="{{ path('hostings_index') }}">Hosting users</a>
                    <a class="dropdown-item" href="{{ path('carpool_index') }}">Carpool users</a>
                </div>
            </div>
            <a class="btn btn-info" href="{{path('user_index')}}" >Users</a>
            <a class="btn btn-info" href="{{path('admin_category')}}">Category</a>
            <a class="btn btn-info" href="{{path('admin_ads')}}">Ads</a>
            <a class="btn btn-info" href="{{path('admin_deals')}}">Deals</a>
            <a class="btn btn-info" href="{{path('admin_cafe')}}">Cafe</a>
            <a class="btn btn-info" href="{{path('admin_hosting')}}">Hosting</a>
            <a class="btn btn-info" href="{{path('admin_meetup')}}">Meetup</a>
            <a class="btn btn-info" href="{{path('admin_voyage')}}">Voyages</a>
            <a class="btn btn-info" href="{{path('jms_jobs_overview')}}">Job</a>
        </div>
    </div>
    <div class="mt-3">
        {% block admin %}

        {% endblock %}
    </div>

{% endblock %}